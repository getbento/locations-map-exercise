const map = {
  SET_DATA: 'map/SET_DATA',
  SET_ACTIVE_LOCATION: 'map/SET_ACTIVE_LOCATION',
  ERROR: 'map/ERROR'
};

const loader = {
  SHOW_LOADER: 'loader/SHOW_LOADER',
  HIDE_LOADER: 'loader/HIDE_LOADER'
};

export default {
  map,
  loader
};
